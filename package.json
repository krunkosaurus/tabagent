{
  "name": "tabagent",
  "version": "0.1.7",
  "private": true,
  "description": "Universal AI Browser Agent Chrome Extension (MV3) with Z.AI coding-plan support",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "clean": "rm -rf dist",
    "test": "node tests/security.mjs",
    "test:browser": "npm run build && node tests/browser.mjs"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "0.28.2",
    "playwright": "1.63.0",
    "typescript": "^5.7.0"
  }
}
